to configure kde do the following:

move bottom bar to the top (or leave at the bottom, how you like)

resize it to 20 (smallest size)
set not floating style

map alt+q -> close window

configure external monitors
